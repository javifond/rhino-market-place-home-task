{
  "name": "rhino-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.6.5",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "check-types": "turbo check-types",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.8.0"
  }
}
